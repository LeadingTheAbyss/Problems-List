| Problem | Rating | Style | Solution Idea | Implementation |
|--------|-------|---------------|---------------------|------|
| [2110D](https://codeforces.com/contest/2110/problem/D) | 1700 |Binary Search on Answer | <details><summary>Show</summary><ul><li>The current batteries we have must be <= the number of batteries we need to cross an edge, inorder to successfully move.</li><li>The minimum cost needed is the cost of the largest edge used while travelling from node `1` to `n` using this path.</li><li>At index `i` let's say the maximum number of batteries we could have till this point is `x`, then inorder to cross all the paths, having `y` number of batteries is enough, where `y <= x` and `y = The maximum edge on this path`</li><li>Eg: If we have 20 batteries right now and need 10 to cross the next edge, we realistically only need 10 batteries, the rest is the maximum possible number of batteries we could have so far (incase an edge demanded more batteries)</li><li>Inorder to minimize the length of the maximum edge, we can simply use Binary Search on Answer where `mid = Largest value of an edgethat we're allowed to pick.`</li></ul></details> | [Code](../Codes/2110D.cpp) |

