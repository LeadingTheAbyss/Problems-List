| Problem | Rating | Solution Idea | Implementation |
|--------|--------|---------------|----------------|
| [1280A](https://codeforces.com/contest/1280/problem/A) | 1700 | <details><summary>Show</summary><ul><li>Simulate how the length of the string increases on each step.</li><li>Every time we try to extend the string at some index <code>i</code>, our string extends by <code>(current_length - l) * (S[i] - 1)</code>, where <code>S[i]</code> is the value at the current index.</li><li>We use <code>S[i] - 1</code> because the suffix is already present once, so we only count the additional copies.</li><li><b>Note:</b> We only need to increase the length of string <code>S</code> till <code>1e6</code> since that is the maximum constraint of <code>x</code>.</li></ul></details> | [Code](../Codes/1280A.cpp) |
| [1690F](https://codeforces.com/contest/1690/problem/F) | 1700 | <details><summary>Show</summary><ul><li>For a cycle of length `m`, after every operation the letters on that cycle rotate by `1`.</li><li>After `k` operations, the string has rotated by `k` moves and if the string comes back to its original form after `k` moves, it will also come back after `2k, 3k, ...` moves.</li><li>So for every cycle we find of length `m`, simply check the minimum operations required by checking if we get the original string after rotating by all the divisors of `m`.</li></ul></details> | [Code](../Codes/1690F.cpp) |
