| Problem | Rating | Additional Topics | Solution Idea | Implementation |
|--------|---------|---------|---------------------|------|
| [2147D](https://codeforces.com/contest/2147/problem/D) | 1700 | - | <details><summary>Show</summary>Count frequencies + Its optimal to always use all your moves on the minimum elements one by one.</details> | [Code](../Codes/2147D.cpp) |
| [2227E](https://codeforces.com/contest/2227/problem/E) | 1400 | <details><summary>Prefix/Suffix Array</summary></details> | <details><summary>Show</summary><ul><li>All numbers from the left get shifted if the current element shifts right, like if some element at height 2 shifts right at index i, it means for all `j < i` where `a[j] > 2`, these cubes get shifted to the right as well.</li><li>So we just need to find the **suffix min** and check how the elements get shifted towards the right.</li><li>We use **prefix sums** on **suffix mins** in order to find the the number of cubes that are blocked vs move right.</li><li>Finally, we try the optimal operation, decreasing a single `a[i]` and recompute the contribution locally using **binary search** over the suffix min structure to **maximize total movement**.</li></ul></details> | [Code](../Codes/2227E.cpp) |
| [2232C2](https://codeforces.com/contest/2232/problem/C2) | - | - | <details><summary>Show</summary><ul><li>Introverts should occupy any table which is empty, we pick this greedily.</li><li>Extroverts should occupy any table which has people already seated on it.</li><li>Ambiverts can seat on occupied as well as non-occupied tables, whichever minimizes the number of people being removed, this is not picked greedily.</li><li>We can use variables : <ol><li>`eS` : Number of empty seats.</li><li>`eT` : Number of empty tables.</li><li>`hold` : Count of seated ambiverts which could be converted into introverts as well (incase we need to seat some extroverts).</ul></details> | [Code](../Codes/2232C2.cpp) |
| [1678B2](https://codeforces.com/contest/1678/problem/B2) | 1800 | - | <details><summary>Show</summary><ul><li>We can rewrite the string as the number of continuous characters. For example : `"1100001"` can be re-written as an array like `[2, 5, 1]`.</li><li><ol><li>For series like `Odd, Odd` we can convert them to `Even, Even` in one operation.</li><li>For series like `Odd, Even` or `Even, Odd` we try to shift elements from either one of them to the other.</li></ol></li><li>The sum of all elements from the array must be even, so for any odd element in it, there must be another odd element in the array as well.</li><li>A trivial fact is that if the elements at the edge are odd then we need to shift their characters inwards to the next odd segments so we can just start from the beginning of the array and move in one direction.</li><li>Inorder to count the minimum number of such possible subsegments we just need to keep merging the smaller characters from the array into the bigger ones greedily.</li></ul></details> | [Code](../Codes/1678B2.cpp) |
