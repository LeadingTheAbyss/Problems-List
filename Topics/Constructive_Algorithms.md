| Problem | Rating | Additional Topics | Solution Idea | Implementation |
|--------|---------|---------|---------------------|------|
| [1530D](https://codeforces.com/contest/1530/problem/D) | 1600 | - | <details><summary>Show</summary><ul><li>We need to track the positions at which any number in `a` repeats.</li><li>Secondly we also track the numbers which are not present in the array at all. For eg in : `a = [6, 4, 6, 2, 4, 5, 6]`, `1` isn't considered anywhere.</li><li>Then we firstly try to assign as many positions where `a[i] != b[i]` within themselves so it doesn't reduce our total score.</li><li>Lastly we cycle the numbers at which `b[i] == i` as its explicitly mentioned for any `b[i]` it must not be equal to `i`.</li></ul></details> | [Code](../Codes/1530D.cpp) |
