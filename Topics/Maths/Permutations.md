| Problem | Rating | Solution Idea | Implementation |
|--------|-------|------------------|------|
| [1585D](https://codeforces.com/contest/1585/problem/D) | 1900 | <details><summary>Show</summary><ul><li>For any operation of moves `i → j → k → i` there are an even number of moves performed : `swap(k, i)` and `swap(j, i)`.</li><li>So if there are an even number of moves to be performed, we can successfully sort the permutation else we can't.</li><li>A move needs to be performed for every inversion i.e `i < j` and `a[i] > a[j]`. So we just count the number of such inversions and check whether this count is even or odd.</li><li>**Note :** If duplicates exist, we can swap equal elements to flip parity while keeping the array unchanged, so any permutation parity becomes achievable.</li></ul></details> | [Code](../Codes/1585D.cpp) |
